{
  "scripts": {
    "test": "run test",
    "build": "parcel build",
    "start": "parcel",
    "clean": "rm -rf ./dist && parcel"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
